services:
    flowise-prod:
        build:
            context: .
            dockerfile: Dockerfile
        restart: always
        env_file:
            - .env
        ports:
            - '3001:3001'
        volumes:
            - ~/.flowise-prod:/root/.flowise
        entrypoint: ['pnpm', 'start']
