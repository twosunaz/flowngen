name: Deploy to DigitalOcean

on:
    push:
        branches:
            - release

jobs:
    deploy:
        runs-on: ubuntu-latest

        steps:
            - name: 📥 Checkout repo
              uses: actions/checkout@v3

            - name: 🔐 Set up SSH
              run: |
                  mkdir -p ~/.ssh
                  echo "${{ secrets.DO_SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
                  chmod 600 ~/.ssh/id_rsa

                  echo "📡 Adding host to known_hosts..."
                  ssh-keyscan -H 165.227.28.110 >> ~/.ssh/known_hosts || {
                    echo "❌ ssh-keyscan failed"
                    exit 1
                  }

            - name: 🛠️ Generate .env.production from GitHub Secrets
              run: |
                  cat <<EOF > .env.production
                  # Server
                  PORT=3001

                  # Storage
                  DATABASE_TYPE=${{ secrets.DATABASE_TYPE }}
                  DATABASE_HOST=${{ secrets.DATABASE_HOST }}
                  DATABASE_PORT=${{ secrets.DATABASE_PORT }}
                  DATABASE_NAME=${{ secrets.DATABASE_NAME }}
                  DATABASE_USER=${{ secrets.DATABASE_USER }}
                  DATABASE_PASSWORD=${{ secrets.DATABASE_PASSWORD }}
                  DATABASE_SSL=${{ secrets.DATABASE_SSL }}
                  DATABASE_SSL_KEY_BASE64=${{ secrets.DATABASE_SSL_KEY_BASE64 }}

                  # Email system
                  SMTP_HOST= ${{ secrets.SMTP_HOST }}
                  SMTP_PORT= ${{ secrets.SMTP_PORT }}
                  SMTP_USER= ${{ secrets.SMTP_USER }}
                  SMTP_PASS= ${{ secrets.SMTP_PASS }}
                  EMAIL_FROM= ${{ secrets.EMAIL_FROM }}
                  VERIFICATION_URL= ${{ secrets.VERIFICATION_URL }}

                  # File system
                  BLOB_STORAGE_PATH=/root/.flowise/storage
                  APIKEY_PATH=/root/.flowise
                  SECRETKEY_PATH=/root/.flowise
                  LOG_PATH=/root/.flowise/logs
                  EOF
            - name: 📦 Upload .env.production to server
              run: scp -o StrictHostKeyChecking=no .env.production root@165.227.28.110:/root/flowngen/.env.production

            - name: 🚀 SSH and deploy with keep-alive
              run: |
                  ssh root@165.227.28.110 << 'EOF'
                    set -e
                    echo "📁 Navigating to project directory..."
                    cd ~/flowngen

                    echo "🔄 Pulling latest changes..."
                    git pull origin release

                    docker-compose -f docker-compose.prod.yml down
                    docker system prune -a --volumes --force

                    echo "🛠️ Building Docker containers..."
                    ( while true; do echo "🌀 Still deploying at \$(date)..."; sleep 60; done ) & 
                    KEEP_ALIVE_PID=$!
                    
                    sleep 2

                    docker-compose -f docker-compose.prod.yml up --build -d

                    kill $KEEP_ALIVE_PID
                    echo "✅ Deployment completed!"
                  EOF
